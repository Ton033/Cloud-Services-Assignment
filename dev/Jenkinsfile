pipeline {

    agent any

    stages {

        stage("build") {

            steps{
                echo 'building the app test'
            }
        }

        stage("test") {

            steps{
                echo 'testing the test'
            }
        }

        stage("deploy") {

            steps{
                echo 'deploy test'
            }
        }

    }
}
